---
title: "Example Repeated Section 1"
format: pdf
editor: visual
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)

library(tidyverse)
library(pander)

```


# Intro

Example of how to automate a repeated section.

```{r}

N <- 500

df <- data.frame(Subsection = sample(LETTERS[1:3],N, replace = TRUE),
                 x = rnorm(N))

```


# The example

```{r, include = FALSE}

out <- NULL

for( i in sort( unique(df$Subsection) )  ){
  
  
  
  dftemp <- df%>%filter(Subsection == i)
  
  out <- c(out, knitr::knit_child("Child_RepeatedSection1.qmd") )
  
  
  
}



```

`r paste0(out,collapse = "\n")`
